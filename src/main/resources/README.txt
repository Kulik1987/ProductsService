for running:
    You need Postgres to start project
    Running the app via the IDE
    for the first run, the parameter: initialization-mode: always
    for second and other run? the parameter: initialization-mode: never